{
  "name": "forever-mern-stack",
  "version": "1.0.0",
  "description": "Premium MERN eCommerce Storefront",
  "main": "backend/server.js",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "node backend/server.js",
    "backend": "cd backend && npm install",
    "frontend": "cd frontend && npm install --include=dev && npm run build",
    "admin": "cd admin && npm install --include=dev && npm run build",
    "heroku-postbuild": "npm run backend && npm run frontend && npm run admin"
  },
  "keywords": [
    "mern",
    "ecommerce",
    "heroku",
    "deployment"
  ],
  "author": "Antigravity",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  }
}
